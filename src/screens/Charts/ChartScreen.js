import React, { Component } from "react";
import styles from "./ChartScreen.scss";

class Chartss extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div className={styles.chartss}>
        
      </div>
    );
  }
}

export default Chartss;
